// import { UserInfo } from './userInfo';

// export class SingleUserInfo extends UserInfo {
//     constructor(username: string, address: string, email: string, phoneNumber: string, roles: string) {
//         super(username, address, email, phoneNumber, roles);
//     }
// }